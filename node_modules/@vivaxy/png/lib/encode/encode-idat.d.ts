import { COLOR_TYPES } from '../helpers/color-types';
export default function encodeIDAT(data: number[], width: number, height: number, colorType: COLOR_TYPES, depth: number, interlace: number, palette?: [number, number, number, number][]): Uint8Array;
