import { COLOR_TYPES } from '../helpers/color-types';
export default function decodeIDAT(deflatedData: Uint8Array, interlace: number, colorType: COLOR_TYPES, width: number, height: number, depth: number, palette?: [number, number, number, number][], transparent?: [number, number, number, number]): number[];
