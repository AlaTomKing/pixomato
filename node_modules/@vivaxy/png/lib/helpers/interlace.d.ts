declare type Image = {
    passWidth: number;
    passHeight: number;
    passIndex: number;
};
export declare function buildImages(interlace: number, width: number, height: number): Image[];
export declare function getPixelIndex(interlace: number, width: number, passIndex: number, widthIndex: number, heightIndex: number): number;
export {};
